# Neon Mix

게임하면서 앱별 소리를 빠르게 조절하는 Windows용 오디오 믹서입니다.

## 실행

`실행 파일 v2` 폴더의 `NeonMix.exe`를 더블클릭하세요. 이전 버전이 실행 중이면 트레이 아이콘을 우클릭하여 ‘종료’한 뒤 새 버전을 실행하세요. 현재 PC에 설치된 .NET 10 Windows Desktop 런타임을 사용합니다. 다른 PC에는 이 런타임이 필요합니다. 실행 파일 폴더 안의 파일을 함께 보관하세요.

## 사용

- 앱에서 소리를 한 번 재생하면 1초 내외로 해당 앱 카드가 추가됩니다. 설치된 모든 앱을 표시하는 목록은 아닙니다.
- 채널 목록은 가로 한 줄로 표시됩니다. 휠을 돌리거나 휠 버튼(가운데 버튼)을 누른 채 좌우로 드래그하면 목록이 이동합니다. 창 폭보다 목록이 길 때 아래 스크롤바로도 이동할 수 있습니다. 전체 출력 MASTER 채널은 오른쪽에 고정됩니다.
- 볼륨 페이더 손잡이를 위아래로 드래그합니다. 페이더에 키보드 초점을 두면 방향키로 1%씩, Home/End로 0%/100%를 설정할 수 있습니다. 페이더 위에서도 휠은 목록을 이동시키며 볼륨은 바꾸지 않습니다.
- 페이더 눈금과 숫자는 Windows 볼륨(0~100%)입니다. 옆의 PEAK 미터는 실제 오디오 피크를 나타냅니다. 앱 세션이 여러 개면 가장 큰 피크를 표시하며, 신호를 읽을 수 없는 세션은 불이 켜지지 않습니다.
- 각 채널의 음소거 버튼으로 해당 앱만 음소거합니다. 같은 프로세스 이름의 여러 세션은 함께 조절합니다. 표시 수치는 세션 볼륨의 평균이며 페이더를 움직이면 같은 값으로 맞춥니다.
- MASTER 채널은 Windows의 기본 멀티미디어 출력 장치를 조절합니다. 개별 앱 채널은 연결된 모든 활성 출력 장치의 앱 세션을 포함하므로, 다른 출력 장치로 보낸 앱은 MASTER 페이더의 영향을 받지 않을 수 있습니다.
- 기본 열기/숨기기 단축키는 Ctrl+Alt+M입니다. 사용 중이면 Ctrl+Alt+F9, Ctrl+Alt+F10, Alt+Shift+M 중 사용 가능한 조합으로 자동 전환하며 창 아래에 표시됩니다. 설정에서 변경할 수 있습니다.
- 창이 열려 있거나 최소화되어 있으면 Alt+Tab으로 전환할 수 있습니다. 트레이로 숨긴 상태에서는 단축키나 트레이 아이콘 더블클릭으로 엽니다.
- Esc, 창의 ×, ‘트레이로 숨기기’는 앱을 종료하지 않고 숨깁니다. 완전히 종료하려면 트레이 아이콘을 우클릭하고 ‘종료’를 선택하세요.
- ‘항상 다른 창 위에 표시’와 단축키 선택은 설정에서 저장합니다.

## Fn 키와 게임 화면

Fn 키는 일반 단축키의 보조 키로 직접 등록하지 않습니다. 키보드의 Fn/Fn Lock 설정으로 F9 또는 F10이 입력되도록 하고 Ctrl+Alt+F9/F10 조합을 사용하세요. 키보드 제조사 소프트웨어에서 Fn 조합을 지정할 수 있다면 앱의 단축키를 보내도록 설정할 수도 있습니다.

이 앱은 독립된 데스크톱 창입니다. 게임 내부에 삽입되는 오버레이는 아닙니다. 창 모드/테두리 없는 전체화면에서 사용하기 적합하며, 독점 전체화면에서는 Alt+Tab으로 전환해야 할 수 있습니다. 실제 게임별 단축키·전체화면 호환성은 별도 확인이 필요합니다.

앱의 볼륨은 Windows 오디오 세션에 즉시 적용됩니다. 설정 파일은 `%LOCALAPPDATA%\NeonMix\settings.json`에 저장합니다. 볼륨 재시작 복원은 Windows와 해당 앱의 오디오 세션 동작을 따릅니다. 자동 시작은 등록하지 않습니다.

## 개발 및 확인

- 빌드: `dotnet build -c Release`
- 오디오 테스트: `NeonMix.exe --self-test` — 무음 테스트 프로세스를 생성해 볼륨/음소거 설정을 확인하고 원래 값으로 복원합니다. 다른 앱의 볼륨은 바꾸지 않습니다.
- 실제 화면 테스트: `NeonMix.exe --smoke-test` — 실제 앱 목록과 단축키 등록, 숨기기/열기, 반복 갱신을 확인하고 종료합니다.
- 디자인 예시: `NeonMix.exe --preview` — 예시 앱으로 채운 화면을 저장하고 종료합니다. 실제 감지 결과와는 다릅니다.

Windows API 참고: [앱 오디오 세션](https://learn.microsoft.com/en-us/windows/win32/api/audiopolicy/nn-audiopolicy-iaudiosessionmanager2), [전역 단축키](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-registerhotkey).
